# please do not place multiple lines on one line
set a = 0; set b = 1

/*
don't actually use this brace style,
just do 'forever {'
*/
forever \
{
    set nxt = (
        + $a $b
    )
    print $nxt
    set a = $b
    set b = $nxt
}

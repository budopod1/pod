set n = (int (inputline "Number? "))

set prime = (all :(each (range 2 $n) {% $n $0}))
# or perhaps in the future
# set prime = (every (map (range 2 $n) {% $n $0}))

print (choose $prime "prime" "not prime")

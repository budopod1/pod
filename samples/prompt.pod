set PROMPT = {
    set dir = (cwd)
    if (assign homerel (relpath $dir ~)) {
        set dir = (if (= $homerel .) {_ ~} else {. ~/ $homerel})     
    }
    . (style (whoami >..) bold green) ":" (style $dir bold blue) "$ "
}
